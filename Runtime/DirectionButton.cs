namespace CollisionBear.InputState
{
    // This enum will be used in menus/ui to simplify input code for it. The input devices themselves will be responsible to determine how these are set
    public enum DirectionButton : int
    {
        Up = 0,
        Down = 1,
        Left = 2,
        Right = 3
    }
}